import { WorldEntity } from "../entities/Entity";
import { BaseBehavior } from "./BaseBehavior";

export class OffensiveBehavior extends BaseBehavior {
    update(dt: number, entity: WorldEntity): void {
        
    }
}