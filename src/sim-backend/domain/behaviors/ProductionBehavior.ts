import { WorldEntity } from "../entities/Entity";
import { BaseBehavior } from "./BaseBehavior";

export class ProductionBehavior extends BaseBehavior{
    update(entity: WorldEntity): void {
        
    }
}